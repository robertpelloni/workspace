{
  "description": "Template configuration for this project. See https://www.codebuff.com/config for all options.",
  "startupProcesses": [
    {
      "name": "watch",
      "command": "echo 'No default watch process configured. Add project-specific processes as needed.'",
      "enabled": false,
      "stdoutFile": "logs/watch.log"
    }
  ],
  "fileChangeHooks": [
    {
      "name": "lint",
      "command": "echo 'No default linter configured. Add project-specific hooks as needed.'",
      "enabled": false
    }
  ],
  "maxAgentSteps": 25,
  "llm_config": {
    "api_base": "http://localhost:4000/v1",
    "api_key": "sk-1234",
    "model": "antigravity"
  }
}
