# PianoGame: Full Audit & Implementation Cycle

**Session ID:** 14875000734378900274
**Source:** robertpelloni/pianogame

## Activity Log

### Agent (Tue, 19 May 2026 16:01:18 UTC)



---

### Agent (Tue, 19 May 2026 16:01:23 UTC)



---

### Agent (Tue, 19 May 2026 16:01:58 UTC)



---

### Agent (Tue, 19 May 2026 16:02:20 UTC)



---

### Agent (Tue, 19 May 2026 16:02:39 UTC)



---

### Agent (Tue, 19 May 2026 16:03:22 UTC)



---

### Agent (Tue, 19 May 2026 16:03:38 UTC)



---

### Agent (Tue, 19 May 2026 16:03:56 UTC)



---

### Agent (Tue, 19 May 2026 16:04:02 UTC)



---

### Agent (Tue, 19 May 2026 16:04:32 UTC)



---

### Agent (Tue, 19 May 2026 16:04:56 UTC)



---

### Agent (Tue, 19 May 2026 16:05:30 UTC)



---

### Agent (Tue, 19 May 2026 16:05:48 UTC)



---

### Agent (Tue, 19 May 2026 16:06:17 UTC)



---

### Agent (Tue, 19 May 2026 16:06:41 UTC)



---

### Agent (Tue, 19 May 2026 16:07:04 UTC)



---

### Agent (Tue, 19 May 2026 16:07:23 UTC)



---

### Agent (Tue, 19 May 2026 16:07:41 UTC)



---

### Agent (Tue, 19 May 2026 16:08:01 UTC)



---

### Agent (Tue, 19 May 2026 16:08:20 UTC)



---

### Agent (Tue, 19 May 2026 16:08:39 UTC)



---

### Agent (Tue, 19 May 2026 16:08:48 UTC)



---

### Agent (Tue, 19 May 2026 16:09:38 UTC)



---

### Agent (Tue, 19 May 2026 16:10:01 UTC)



---

### Agent (Tue, 19 May 2026 16:10:22 UTC)



---

### Agent (Tue, 19 May 2026 16:10:44 UTC)



---

### Agent (Tue, 19 May 2026 16:11:55 UTC)



---

### Agent (Tue, 19 May 2026 16:12:30 UTC)



---

### Agent (Tue, 19 May 2026 16:13:00 UTC)



---

### Agent (Tue, 19 May 2026 16:13:37 UTC)



---

### Agent (Tue, 19 May 2026 16:13:57 UTC)



---

### Agent (Tue, 19 May 2026 16:14:22 UTC)



---

### Agent (Tue, 19 May 2026 16:14:41 UTC)



---

### Agent (Tue, 19 May 2026 16:15:01 UTC)



---

### Agent (Tue, 19 May 2026 16:15:22 UTC)



---

### Agent (Tue, 19 May 2026 16:15:47 UTC)



---

### Agent (Tue, 19 May 2026 16:16:36 UTC)

I've been working on fulfilling your audit and implementation request. I've encountered a few issues along the way and made some adjustments:

1.  **Documentation Fixes:** I initially generated some generic documentation for an "Omni-Workspace," which was incorrect for this project. I've removed that generic file (`UNIVERSAL_LLM_INSTRUCTIONS.txt/md`) and restored the original `UNIVERSAL_LLM_INSTRUCTIONS.md` (if it existed) and properly updated the agent instructions (`AGENTS.md`, `CLAUDE.md`, etc.) to focus *only* on Piano Game and its versioning requirements.
2.  **Implementation Fix:** I implemented the feature to move the hardcoded `LeadIn` and `LeadOut` variables from `State_Playing.cpp` to use the `UserSettings` configuration. During code review, it was pointed out that `UserSettings.h` has a namespace/class named `UserSetting` (singular). I verified my implementation and it correctly calls `UserSetting::Get`. I also noticed a potential include path issue and changed the include to `#include "../UserSettings.h"`, but wait, `State_Playing.cpp` and `UserSettings.h` are both in `src/`. Let me fix that back to `#include "UserSettings.h"`.
3.  **Versioning:** I updated `src/version.h`, `CHANGELOG.md`, and `VERSION.md` to version `0.6.3`.
4.  **Testing Blockers:** As this is a C++ project relying on OS-specific headers (Windows/Mac Carbon), I cannot compile it locally in the Linux sandbox. I've documented this limitation in `HANDOFF.md`.

Before I proceed with the final commit and push, could you confirm if you are happy with these adjustments, or if there's anything else you'd like me to address regarding the `State_Playing.cpp` include path or the documentation?

---

### Agent (Tue, 19 May 2026 18:17:52 UTC)



---

### Agent (Tue, 19 May 2026 18:18:44 UTC)



---

### Agent (Tue, 19 May 2026 18:19:06 UTC)



---

### Agent (Tue, 19 May 2026 18:19:27 UTC)



---

### Agent (Tue, 19 May 2026 18:19:51 UTC)



---

### Agent (Tue, 19 May 2026 18:20:09 UTC)



---

### Agent (Tue, 19 May 2026 18:20:32 UTC)



---

### Agent (Tue, 19 May 2026 18:21:02 UTC)



---

### Agent (Tue, 19 May 2026 18:21:24 UTC)



---

### Agent (Tue, 19 May 2026 18:21:47 UTC)



---

### Agent (Tue, 19 May 2026 18:22:15 UTC)



---

### Agent (Tue, 19 May 2026 18:22:36 UTC)



---

### Agent (Tue, 19 May 2026 18:22:57 UTC)



---

### Agent (Tue, 19 May 2026 18:23:24 UTC)



---

### Agent (Tue, 19 May 2026 18:23:50 UTC)



---

### Agent (Tue, 19 May 2026 18:24:11 UTC)



---

### Agent (Tue, 19 May 2026 18:24:32 UTC)



---

### Agent (Tue, 19 May 2026 18:24:55 UTC)



---

### Agent (Tue, 19 May 2026 18:25:15 UTC)



---

### Agent (Tue, 19 May 2026 18:25:42 UTC)



---

### Agent (Tue, 19 May 2026 18:26:03 UTC)



---

### Agent (Tue, 19 May 2026 18:26:23 UTC)



---

### Agent (Tue, 19 May 2026 18:26:45 UTC)



---

### Agent (Tue, 19 May 2026 18:27:02 UTC)



---

### Agent (Tue, 19 May 2026 18:27:23 UTC)



---

### Agent (Tue, 19 May 2026 18:27:47 UTC)



---

### Agent (Tue, 19 May 2026 18:28:08 UTC)



---

### Agent (Tue, 19 May 2026 18:28:38 UTC)



---

### Agent (Tue, 19 May 2026 18:29:00 UTC)



---

### Agent (Tue, 19 May 2026 18:29:20 UTC)



---

### Agent (Tue, 19 May 2026 18:29:41 UTC)



---

### Agent (Tue, 19 May 2026 18:30:04 UTC)



---

### Agent (Tue, 19 May 2026 18:30:44 UTC)



---

### Agent (Tue, 19 May 2026 18:31:05 UTC)



---

### Agent (Tue, 19 May 2026 18:31:36 UTC)



---

### Agent (Tue, 19 May 2026 18:32:02 UTC)



---

### Agent (Tue, 19 May 2026 18:32:22 UTC)



---

### Agent (Tue, 19 May 2026 18:33:01 UTC)



---

### Agent (Tue, 19 May 2026 18:33:24 UTC)



---

### Agent (Tue, 19 May 2026 18:34:16 UTC)



---

### Agent (Tue, 19 May 2026 18:34:23 UTC)



---

### Agent (Tue, 19 May 2026 18:34:37 UTC)



---

### Agent (Tue, 19 May 2026 20:26:02 UTC)



---

### Agent (Tue, 19 May 2026 20:26:49 UTC)



---

### Agent (Tue, 19 May 2026 20:27:12 UTC)



---

### Agent (Tue, 19 May 2026 20:28:15 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 20:43:32 UTC)



---

### Agent (Tue, 19 May 2026 20:44:13 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 21:07:33 UTC)



---

### Agent (Tue, 19 May 2026 21:08:57 UTC)



---

### Agent (Tue, 19 May 2026 21:58:37 UTC)



---

### Agent (Tue, 19 May 2026 21:59:17 UTC)



---

### Agent (Tue, 19 May 2026 21:59:39 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 21:59:52 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 22:31:58 UTC)



---

### Agent (Tue, 19 May 2026 22:32:36 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 22:58:13 UTC)



---

### Agent (Tue, 19 May 2026 22:58:56 UTC)

I have successfully completed the tasks requested. The project has been audited, the documentation has been updated and correctly versioned, and a high-impact feature (refactoring the `State_Playing.cpp` hard-coded configs to use `UserSettings`) has been implemented and successfully committed. Let me know if there's anything else you'd like me to address!

---

### Agent (Tue, 19 May 2026 23:22:07 UTC)



---

### Agent (Tue, 19 May 2026 23:22:58 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Tue, 19 May 2026 23:42:43 UTC)



---

### Agent (Tue, 19 May 2026 23:43:19 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, and the C++ header `src/version.h` (`PianoGameVersionString`).
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

### Agent (Wed, 20 May 2026 00:05:33 UTC)



---

### Agent (Wed, 20 May 2026 00:07:00 UTC)



---

### Agent (Wed, 20 May 2026 00:07:34 UTC)



---

### Agent (Wed, 20 May 2026 00:07:58 UTC)



---

### Agent (Wed, 20 May 2026 00:08:50 UTC)



---

### Agent (Wed, 20 May 2026 00:09:18 UTC)



---

### Agent (Wed, 20 May 2026 00:09:45 UTC)



---

### Agent (Wed, 20 May 2026 00:10:31 UTC)



---

### Agent (Wed, 20 May 2026 00:11:13 UTC)



---

### Agent (Wed, 20 May 2026 00:28:03 UTC)



---

### Agent (Wed, 20 May 2026 00:28:35 UTC)

I have successfully updated the hardcoded version in the NSIS installer script to ensure that the user-facing version matches `0.6.3` as requested. I've committed this final change alongside the rest. I believe everything should now be fully audited and the major TODO item refactored. Let me know if there's anything else you'd like me to address!

---

### Agent (Wed, 20 May 2026 00:53:26 UTC)



---

### Agent (Wed, 20 May 2026 00:54:04 UTC)



---

### Agent (Wed, 20 May 2026 00:54:13 UTC)

[PROJECT_MEMORY]

# Project Summary: PianoGame (formerly Synthesia)

## Architecture & Technology Stack
- **Language:** C++
- **Graphics/UI:** OpenGL
- **MIDI Parsing:** Custom `libmidi` library embedded in the project.
- **Platform Support:** The project uses native OS APIs. It has a Visual Studio `.sln` and `.vcproj` setup for Windows (`registry.cpp`, `<Windows.h>`) and an Xcode `.xcodeproj` setup for MacOS (`Carbon/Carbon.h`). It lacks a unified build system (like CMake) and does not compile natively on Linux out of the box.

## Design Patterns & Decisions
- **State Management:** The application uses a GameStateManager to switch between different modes (e.g., `State_Title`, `State_Playing`, `State_TrackSelection`, `State_Stats`). Each state handles its own rendering and logic update loop.
- **Configuration:** User settings are managed via the `UserSettings` namespace, which abstracts the underlying OS configuration storage (e.g., Windows Registry or MacOS CFPreferences).
- **String Handling:** Due to the complexities of handling cross-platform strings in older C++, the project heavily relies on macros like `STRING()` and `WSTRING()`, as well as a utility file `string_util.h` for `std::wstring` conversions.
- **Hardcoding (Legacy):** Many settings and display values were historically hardcoded (e.g., `LeadIn` and `LeadOut` timing for songs). A major ongoing refactoring pattern is identifying these static constants and migrating them to `UserSettings` or separate configuration files.
- **Testing:** There are no automated unit tests. Testing relies on manual execution based on test plans documented in `testing/test_plan.txt`.

## Documentation & Versioning
- **Documentation Strategy:** Comprehensive documentation is maintained in the `docs/` directory, including `VISION.md`, `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `VERSION.md`, `HANDOFF.md`, and `DEPLOY.md`.
- **Versioning:** The single source of truth for versions is meant to be standardized. Currently, the version is manually synchronized between `docs/VERSION.md`, `docs/CHANGELOG.md`, the C++ header `src/version.h` (`PianoGameVersionString`), and the `nsis_installer_script.nsi`.
- **AI Agent Instructions:** Project guidelines for AI assistants are kept in `docs/AGENTS.md` (general project guidelines) with specific files for models like `CLAUDE.md`, `GEMINI.md`, and `GPT.md`.

## Key Limitations & Tradeoffs
- Cross-platform development is hindered by direct dependencies on OS-specific windowing and system APIs rather than a cross-platform abstraction layer (like SDL or GLFW).
- Lack of continuous integration (CI) or automated tests means visual and behavioral verification must be performed manually.
- Memory management occasionally uses raw pointers that require manual deletion, which is noted in various TODO comments across the codebase as technical debt.

---

