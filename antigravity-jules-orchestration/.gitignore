# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
build/
dist/

# IDE & Editor
.idea/
.vscode/
*.swp
*.swo
*.sublime-*

# OS Files
.DS_Store
Thumbs.db

# Environment (CRITICAL - no secrets in repo)
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Generated & Output
mcp-test-output/
generated-artifacts/
.playwright-mcp/
TRASH/

# Secrets & Keys (CRITICAL)
jules-service-account-key.json
RENDER_KEY_INSTRUCTIONS.md
*.pem
*.key
credentials*.json

# JSON (except required files)
*.json
!package.json
!package-lock.json
!antigravity-mcp-config.json
!templates/*.json
!.claude/*.json
!config/*.json
!orchestrator-api/package.json
!orchestrator-api/package-lock.json
!dashboard/package.json
!dashboard/package-lock.json

# Temporary Status Files
GCLOUD_LOGIN_NEEDED.md
