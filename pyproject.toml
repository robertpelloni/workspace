[project]
name = "workspace"
version = "1.2.5"
description = "Robert Pelloni's unified development ecosystem — monorepo orchestration hub"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.9.5",
]
